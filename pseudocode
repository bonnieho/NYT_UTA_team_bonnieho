Create a search box. Create an id for the search box. 
js to store content of the search box.

Create a list to store number of records. 
Create an array for the values.
store number of records in a js variable.

Start Year (optional)
-Create validation for year entry (user proof)

End Year (optional)
-Create validation for year entry (user proof)

Search Button:

Calls the API and loads the results based on the following variables:

searchValue
records
startYear
endYear

Results will be shown below the search form.

Create the divs to store the results.
js - use either prepend or append to push the results to html.


